Слушать голосовое с 49:20 примерно

Задачи:
1) Разделить метод updateModel() в классе Model, то есть сделать update в Robot'е and Target'e, а потом в моделе вызывать
updates из робота и таргета.
Итог по задаче: сделал, но как будто таргет не нужно обновлять в update Model, поэтому просто в робота закинул метод update
и вызываю его из Model

2) перенести методы из DrawEntity в Renderer. DONE

3) Разделить render() из класса Renderer на два render, так как Robot and Target - разные сущности

Итог задачи: объединил робота и таргет - так показалось проще и вроде логике не вредит особо, по сути таргет
принадлежит роботу, поэтому норм я считаю

4) GameWindow gameWindow = new GameWindow();
   gameWindow.setSize(400,  400);
   Эти две строки вытащить из ApplicationFrame и закинуть во вьюмодел, ну и лог виндоу тоже надо вынести во вьюмодел

5) Про вьюмодел послушать, там вроде понятно, но писать таску словами - много букв лень

6) Бля, че с таймером делать, я так и не понял

7) Во вьюмодел и вью создаются 2 разные модели, сделать их одной, тогда сущности будут не только отрисовываться, но и двигаться

8) В ApplicationFrame надо создавать Model и View, а потом передавать их во ViewModel

9) Переименовать Renderer во View. DONE

10) Взять у Димы update() and moveRobot()
10) надо подумать над следующими задачами (симуляцией бактерий)

